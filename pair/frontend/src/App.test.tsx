import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import { App } from './App';

describe('App', () => {

  test('renders SignUpPage for /signUp path', () => {
    
  });

  test('renders Page404 for unknown paths', () => {
    
  });
});
